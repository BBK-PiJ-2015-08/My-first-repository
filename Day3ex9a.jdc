println ("Please enter some text");
String userInput = new String("");
userInput = readLine();
int count = 0;
int numofChars = userInput.length();
for (int i=0; i<userInput.length(); i++) {
	if ((userInput.substring(i,i+1)).equals("e")) {
		count = count + 1;
	} else {
		if (!(userInput.substring(i,i+1)).equals("e")) {
			count = count;
		}
	}
}
println("Total number of e's is " + count);